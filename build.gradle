allprojects {
	group = "com.att.devops.autoload"
	
	version = "1.0.0-SNAPSHOT"
}

subprojects {
	apply plugin: "java"
	apply plugin: "eclipse"
	
	repositories { 
		mavenCentral() 
	}
	
	sourceCompatibility = 1.7
	targetCompatibility = 1.7
}

task wrapper(type: Wrapper) {
	gradleVersion = '1.12'
}